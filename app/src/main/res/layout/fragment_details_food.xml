<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/primary_background"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    tools:context="com.wes.app.ui.screens.foods.foodsdetails.DetailsFoodsFragment">

    <LinearLayout
        android:padding="16dp"
        android:id="@+id/relativeFood"
        android:background="@color/colorPrimaryDark"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <TextView
            android:gravity="center"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:text="Pupular"
            android:textStyle="bold"
            android:textColor="@color/text_detail"
            android:textSize="@dimen/text_autor_detail"/>
    </LinearLayout>
    <androidx.recyclerview.widget.RecyclerView
        android:layout_below="@+id/relativeFood"
        android:id="@+id/rcvFoodDetail"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager">
    </androidx.recyclerview.widget.RecyclerView>
</RelativeLayout>